// #include "catch.hpp"

// #include <map>
// #include <string>

// // #include "sequence/sequence.h"
// // #include "sequence/collection.h"

// // using std::cout;
// using std::endl;
// using std::string;
// using std::map;
// using std::pair;
// using std::vector;

// class Collection;

// struct MockCollection {
//     string head=""; 
//     string tail=""; 
//     string format="";
//     string full_format="";
//     int count=0, padding=0, first=0, last=0;
// };

// MockCollection get_mock(string head, string tail, string format, string full_format, int count, int padding, int first, int last);

// vector<string> test_collection(Collection c, MockCollection mock, bool quiet);

// vector<string> test_parser(string input_sequence, MockCollection mock, bool quiet=false);
